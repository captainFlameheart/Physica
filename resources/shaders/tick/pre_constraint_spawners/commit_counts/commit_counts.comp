layout(local_size_x = commit_constraint_spawner_counts_local_size) in;
void main()
{
	PRE_TICK_ENTITIES_COMMIT_COUNTS(
		contact_detector_type_base,
		contact_detector_type_count,
		contact_detector_clear_deaths_program_base,
		contact_detector_perform_compaction_program_base,
		contact_detector_tick_entities_program_base,
		contact_detector_local_sizes_flags
	);
}
